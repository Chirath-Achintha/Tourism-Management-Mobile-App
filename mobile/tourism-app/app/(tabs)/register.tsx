import { View, Text } from "react-native";

export default function TabsRegisterScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Register Screen (Tabs)</Text>
    </View>
  );
}
